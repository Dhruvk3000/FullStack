// JavaScript function to show an alert
const element = document.getElementsById(mybtn)
element.addEventListener("click", myFunction);
function showAlert() {
    document.getElementById("demo").innerHTML = "Secret message";
}
